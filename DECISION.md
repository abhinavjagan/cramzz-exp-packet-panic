# Packet Panic decision record

Status: **Pending launch and fourteen-day validation window**

The final decision must be exactly one of:

- `SCALE` — thresholds in `EXPERIMENT.md` were met.
- `ITERATE_ONCE` — the defined partial-signal threshold was met; document the single major variable being changed and run for seven additional days.
- `KILL` — validation thresholds were not met or an immediate-kill condition occurred. Preserve a static case study, remove active sponsor inventory, and do not silently move the goalposts.

Record at decision time:

1. launch and decision timestamps;
2. verified metrics and excluded traffic;
3. distribution effort completed;
4. gross revenue and direct costs;
5. sponsor conversations and commitments;
6. decision with evidence;
7. reusable assets and the next experiment implication.
